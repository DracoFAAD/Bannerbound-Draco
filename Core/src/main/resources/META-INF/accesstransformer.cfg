# Bannerbound Core access transformers.
#
# Expose the package-private GameRules.Type constructor and the GameRules.VisitorCaller
# interface so the mod can register a *custom* game-rule value type (an enum-backed rule).
# NeoForge's bundled AT only opens up register() + BooleanValue/IntegerValue.create(); a
# bespoke Value type needs to build its own Type directly. Used by ForceMaxAgeGameRule so
# `/gamerule forceMaxAge <era>` reads as friendly era presets instead of a raw ordinal.
public net.minecraft.world.level.GameRules$VisitorCaller
public net.minecraft.world.level.GameRules$Type <init>(Ljava/util/function/Supplier;Ljava/util/function/Function;Ljava/util/function/BiConsumer;Lnet/minecraft/world/level/GameRules$VisitorCaller;)V
